{
  "name": "agentes-industriais-front",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Relatório do experimento em React. Reusa src/interfaces/web/app.js — a mesma fonte que o HTML estático embute.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dados": "cd .. && uv run agentes dashboard --run-id e1_v1 --metrics artifacts/runs/e1_v1/metrics.db --e2-run-id e2_v1 --e2-metrics artifacts/runs/e2_v1/metrics.db --seeds complete,s10,x1,s13 --repetition 0 --out docs/relatorio/index.html --data-out frontend/src/data"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
